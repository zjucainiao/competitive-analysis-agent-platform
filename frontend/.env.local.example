# Copy to .env.local — Next.js will load automatically.
# .env.local is git-ignored by next.js default .gitignore.

# Backend FastAPI base URL
NEXT_PUBLIC_API_BASE=http://localhost:8000

# WebSocket base (default = derived from API base by swapping http→ws)
# NEXT_PUBLIC_WS_BASE=ws://localhost:8000
