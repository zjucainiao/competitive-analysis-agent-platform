# 项目约定

## 进度文档维护（必须）

每次修改项目（代码、文档、部署）后，必须同步更新根目录 `PROGRESS.md`：

1. 在「更新日志」当天日期下追加一条改动记录（当天已有日期则合并进去，新日期加在最上面）
2. 若改动影响「当前状态 / 进行中 / 待办」，一并更新
3. 只记录从 2026-07-02 起的改动，不回溯历史

`PROGRESS.md` 是工作日志，不属于 `docs/` 技术参考体系；`docs/` 内禁止出现进度/里程碑类内容。
